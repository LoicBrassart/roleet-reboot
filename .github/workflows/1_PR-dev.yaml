name: Cherry-picked Unit/integration Tests

on:
  pull_request:
    branches:
      - dev
    types: [opened, reopened, synchronize]

jobs:
  check-touched-services:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        services:
          - frontend
          - backend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Filter changed files
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            frontend:
              - 'frontend/**'
            backend:
              - 'backend/**'

      - name: Run ${{ matrix.services }} tests
        if: ${{ steps.filter.outputs[matrix.services] == 'true' }}
        working-directory: ${{ matrix.services }}
        run: |
          npm ci
          npm run test
